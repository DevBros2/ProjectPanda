<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProjectPanda.Views.ProfilePageViews.UserPrivateDetailedInfo.MedicationDetailList">
    <ContentPage.Content>
        <ListView ItemsSource="_Medications">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ContentView>
                            <Frame OutlineColor="Black"  Padding="10"  CornerRadius="7"  VerticalOptions="StartAndExpand">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackLayout Grid.Row="0" Grid.Column="0">
                                        <Image Source="{Binding medicationIcon}"/>
                                    </StackLayout>
                                    
                                    <StackLayout Grid.Row="0" Grid.Column="1">
                                        <Label Text="{Binding titleofmedication}"/>
                                    </StackLayout>

                                    <StackLayout Grid.Row="0" Grid.Column="1">
                                        <Label Text="{Binding dosageofmedication}"/>
                                    </StackLayout>
                                    
                                </Grid>
                            </Frame>
                        </ContentView>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>