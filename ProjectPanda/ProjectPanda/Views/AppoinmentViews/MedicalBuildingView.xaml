<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProjectPanda.Views.MedicalBuildingView"
             BackgroundColor="{StaticResource BackgroundColor}" >
    <ContentPage.Content>

        <ListView x:Name="MedicalBuilding"
                  SeparatorVisibility="None" 
                  HasUnevenRows="True"
                  ItemsSource="{Binding Practices}"
                  IsPullToRefreshEnabled="True"
                  CachingStrategy="RecycleElement"
                  HeightRequest="100"
                  SelectedItem="MedicalBuilding_ItemSelected"
                  ItemSelected="MedicalBuilding_ItemSelected">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ContentView Padding="5">

                            <Frame OutlineColor="LimeGreen"  Padding="10"  CornerRadius="7"  VerticalOptions="StartAndExpand">

                                <Grid Padding="5">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="30"/>
                                        <RowDefinition Height="120"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Grid.ColumnDefinitions>

                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>

                                    </Grid.ColumnDefinitions>

                                    <StackLayout Grid.Row="1" Grid.Column="0" VerticalOptions="StartAndExpand" Margin="0,0,5,0" Orientation="Horizontal">

                                            <StackLayout Grid.Column="0" Grid.Row="1">
                                                <Image Source="{Binding ProfileImage}"  HeightRequest="120"  VerticalOptions="StartAndExpand" HorizontalOptions="CenterAndExpand" Aspect="AspectFit"/>
                                            </StackLayout>
                    
                                                                      
                                    </StackLayout>


                                    <StackLayout Grid.Row="0" Grid.Column="0"  VerticalOptions="StartAndExpand" Margin="3,1,1,10">

                                        <Label Text="{Binding PracticeName}" FontSize="20" VerticalOptions="Start" Style="{DynamicResource HeaderAttributes}" LineBreakMode="NoWrap" />

                                    </StackLayout>

                                    <StackLayout Grid.Row="1" Grid.Column="1" Margin="80,1,1,10">
                                        <Label  Text="{Binding Distance}" TextColor="Green" Style="{DynamicResource InformationLabel}" LineBreakMode="NoWrap"/>
                                    </StackLayout>
                                    

                                </Grid>


                            </Frame>
                        </ContentView>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

    </ContentPage.Content>
</ContentPage>