<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProjectPanda.Pages.AppointmentHistoryFolder.AppoitnmentHistory"
             Title="Appointment History"
             >
    <ContentPage.Content>
        <StackLayout>
            <ListView  x:Name="AppointmentHistory">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout BackgroundColor="#eee"    Orientation="Vertical">
                     
                                <StackLayout Orientation="Horizontal">

                                    <Label Text="{Binding date}" HorizontalOptions="StartAndExpand"  TextColor="#f35e20" />

                                    <Label Text="{Binding time}" HorizontalOptions="EndAndExpand"  TextColor="#503026" />

                                    <Label Text="{Binding doctorSeen}" HorizontalOptions="End"/>

                                </StackLayout>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>